specVersion: "v1beta"
serviceId: "admin-page"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin-page-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
